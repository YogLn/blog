import styled from 'styled-components';

export const MessageWrapper = styled.div`
	width: 80%;
	.more {
		color: #4c30ff;
		text-align: center;
		text-decoration: underline;
		cursor: pointer;
	}
	.leave-message {
		margin-top: 40px;
		width: 100%;
	}
`