import styled from 'styled-components';

export const CommentWrapper = styled.div`
	position: relative;
	.submit {
		position: absolute;
		right: 0;
		top: 120px;
	}
	.comment-list {
		margin-top: 50px;
	}
`